/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.seadn.io','img.reservoir.tools','i.nfte.ai','lh3.googleusercontent.com','randomuser.me'], 
      },
};

export default nextConfig;
